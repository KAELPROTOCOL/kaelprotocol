[package]
name = "swapkit"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "swapkit"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
